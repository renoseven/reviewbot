---
description: reviewbot 的 Rust 编码形状：文件切分、封装、对象化与硬约束
globs: **/*.rs
alwaysApply: false
---

# Rust 编码规范

## 文件与模块

- 一个文件一件事：**不是一个类型一个文件**。同一实现的类型放一起；同一份词汇的纯数据也放一起。不相干的才另起文件
- 纯数据（只有字段、行为跟着别人走）不和实现混写。只作别人字段或变体载荷、自己没有独立行为面的，跟主类型同文件
- 目录下禁止只有 `mod.rs`。`mod.rs` 只写模块文档、`mod` 声明与 `pub use` 再导出，不放类型与实现
- 只有一个文件的模块就写成 `xxx.rs`，不要为它建目录

## 封装

- 除纯数据类型外，字段私有，外部走函数读；不给 setter，除非生命周期需要
- `impl` 按用途分块，顺序：生命周期（构造 / 打开 / 结束）→ 数据访问（读字段）→ 功能（行为）→ 私有辅助
- 跨模块不直接读写别人的字段

## 实体对象化

数据与属于它的行为放在一起；模块之间只靠公开方法与 trait 说话，做到高内聚、低耦合。

- **独立实体必须实例化**：先构造出对象，再对实例调方法。禁止写成关联函数或模块级自由函数，让调用方带着字段到处传
- 构造写在 `impl` 上：必有依赖一次注入，禁止空 `new` 再 `set`、禁止事后补绑、禁止从全局取。不变式在构造里立住，立不住就返回错误。几种合法形态用命名构造分开，不要丢布尔让调用方拼
- 一个类型只为一件事：字段互相配合、方法共用同一组不变式。无关能力另起类型
- 函数参数不超过 5 个（不含 `self`）。超出才收成 context：只装这一次用到的依赖，字段私有、默认只读，禁止塞全局状态
- 共享词汇不是独立实体：不含逻辑，不实例化来干活；行为仍跟主人走

```rust
// ❌ 共享词汇自己做对齐
impl ChangeSet {
    pub fn align(&self, line: u32) -> Option<u32> { ... }
}

// ✅ 行号是 ChangeSet 上的数据，对齐是 Merge 的方法
impl Merge {
    pub fn align(&self, lines: &BTreeSet<u32>, line: u32) -> Option<u32> { ... }
}

// ❌ Tree 只是命名空间，根目录每次当参数传来传去
impl Tree {
    pub fn read(root: &Path, path: &str) -> Result<String, Error> { ... }
}
Tree::read(&root, path)?;

// ✅ 先打开一棵树（根目录是它的状态），再问这棵树
impl Tree {
    pub fn open(root: PathBuf) -> Result<Self, Error> { ... }
    pub fn read(&self, path: &str) -> Result<String, Error> { ... }
}
let tree = Tree::open(root)?;
tree.read(path)?;
```

## 命名

- 用常见英文词。禁止生僻词、口语化名字、自造缩写、为已有名字再造同义词
- 配置字段名说清量纲与主语：带单位的写进名字（`_bytes` / `_ms` / `_tokens`），布尔用肯定式，别用同一个词指两件事

```rust
// ❌ provenance（生僻）；stuff, dump, grab, do_it, helper（口语）
// ✅ align, retry, budget, limit
```

## 依赖

- 已有成熟库的事不要自己写。哈希用 `sha2`，时间用 `chrono`，同类照办

## 错误

- 库内用 `thiserror` 定义可匹配的错误类型，调用方靠类型分支（退出码、是否重试）
- **最外层**（`main.rs`）可以用 `anyhow` 收口、补上下文；`anyhow::Error` 不进 `lib`
- **禁止 `unwrap` / `expect`**。失败用 `?` 或匹配 `Result` 往外传，不要藏进默认值、`ok()` 丢掉、或 `let _ =` 当没发生
- 错误必须可见：能恢复的显式处理，不能恢复的原样上交。禁止把失败收成成功

## 硬约束的代码形状

- **不改外来的数**：原样存、原样发；拿不到就 `None`，禁止填默认值凑
- **不静默降级**：失败就失败，禁止换实现、超限后继续、把错误收成「还能跑」
- **全程串行**：调用前要做额度或上限检查的，不要并发
- 密钥只在内存，不写落盘产物或日志；子进程环境剔除凭据
- 外部命令 argv 是数组，直接 `execve`，一个占位符一个元素，不经 shell
- 瞬时故障才重试（超时 / 5xx / 429）；鉴权、客户端错误、额度不足首次即失败
- 持久化原子写（tmp + rename）；损坏回退上一份完整快照，禁止 catch 之后整次重跑
