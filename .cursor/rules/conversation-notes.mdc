---
description: 蒸馏对话中的关键步骤与决策，并精简保留用户原话以核对意图
alwaysApply: true
---

# 对话记录

有实质进展（决策、实现、排错、改设计）时，把对话蒸馏进 `docs/conversations/`。禁止整段粘贴 transcript；用户原话应精简保留，事后能核对意图。

## 文件

`docs/conversations/<NNN>-<短标题>.md`

- 同一对话只维护一个文件；新开对话另建。
- `<NNN>` 为三位序号，取现有文件最大号 + 1；短标题用 ASCII kebab-case，例如 `001-conversation-notes-rule.md`。文件名不含日期或时间。
- 文件不存在就创建。已有记录先读再改，禁止平行再写一份。不套子目录。

## 写什么

以问题为粒度。每个问题一套，标题写成 `## 短句`（必要时标题下补一句，不单开「问题」段）。套与套之间用 `---` 隔开：

1. **意图**：精简后的用户原话（可缩句、合并重复、去掉寒暄），只看这一段就能核对「当时要的是什么」。用户用编号、「上面那条」等指代时，把所指的具体内容写出来，不要只留下指代。多项要求要么写成连贯短句、不夹「1、2、3」，要么逐项列举。原话没说清做到哪一步时补一句。不要整段 transcript，也不再单开「目标」
2. **步骤**：按顺序列做了什么（改了哪些文件、验证结果），一步一行
3. **决策**：取舍与理由；被否决的方案只留一句「为什么不是」

同一类问题合成一套，不要平行再开一套。同类指围绕同一主题、同一工件或同一决策面的追问、补丁、纠偏。后话并进已有套的意图 / 步骤 / 决策：意图去重后并列原话，步骤按时间追加并压缩过时试错，决策只留仍有效的取舍（被后话推翻的删掉或改写成最终结论）；标题改成仍能概括整套的短句。

不同类问题各自成套、分开写。不要把不同类问题的意图、步骤、决策按栏目合并。同一对话里的新问题：能归进已有套就改那一套，否则追加一套，不另开文件。

文件末尾可有一个 **未决**（`## 未决`）：整份对话还开着的问题与下一步。前面同样用 `---` 与上一套隔开。

改写时压缩旧段落：合并重复、删掉已过时的试错，保留结论与意图依据。密钥、token、完整 prompt、大段日志一律不入库。

## 时机

- 做出不可逆决策或改完一批文件后立刻更新
- 对话结束前再通读一遍，把步骤收成可复述的序列
