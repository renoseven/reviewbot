---
description: 离线、按档、只在最低够用的档测
globs: **/*.rs
alwaysApply: false
---

# 测试规范

## 五档，每档可单独跑

只能靠端到端才验得了的行为 = 依赖没切干净，先切开再测。

| 档 | 测什么 | 依赖 |
|---|---|---|
| 单元 | 纯算法 | 无 FS、无网 |
| 契约 | 接口约定。**真假实现跑同一组用例** | 只起被测那一侧 |
| 组合 | 若干步的输入输出 | 上下游 fixture，边界用假实现 |
| 整装 | 入口到出口跑通、恢复、额度耗尽 | 外部全假 |
| CLI | 输出流、退出码、落盘位置 | 起子进程 |

同一行为**只在够得着它的最低那档测**。

## 怎么写

- crate 同时产出 `lib` 与 `bin`。业务逻辑一律测 `lib`；只有输出流、退出码、落盘位置才起二进制
- 测试必须离线。外部换成假实现，入口就能整套跑完
- 新增行为先归到某一档，再写代码——写不出单独可跑的测试就说明依赖没切干净

```rust
// ❌ 为一段纯算法起整条链路 + 假外部
// ✅ 直接喂输入，断言输出

// ❌ 去重断言「说的是同一类问题」
// ✅ 只在 path 相同、区间相交、正文规范化后逐字相同时合并
```

## 不要测的

- 判不了的不断言（「语义相同」这类；正文不同就不合并）
- 合法空输出测不出来的，不要当失败
- 退出码只描述进程自己跑得怎么样，不把业务结论塞进去
