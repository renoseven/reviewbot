---
description: reviewbot 五档测试：离线、按层、只在最低够用的档测
globs: **/*.rs
alwaysApply: false
---

# 测试规范

## 五档，每档可单独跑

只能靠端到端才验得了的行为 = 依赖没切干净，先切开再测。

| 档 | 测什么 | 依赖 |
|---|---|---|
| 单元 | 设施与阶段内的纯算法：对齐、引文比对、去重、glob、token 估算、脱敏、退避。`domain` 不含逻辑，不在这里堆函数 | 无 FS、无网 |
| 契约 | 各适配器 trait 的行为约定。**真假实现跑同一组用例** | 只起被测那一层 |
| 阶段 | 五个阶段各自的输入输出 | 上下游 fixture，适配器假实现 |
| 整装 | 五阶段串起来、恢复、指纹、预算耗尽 | 适配器全假 |
| CLI 契约 | 输出流分配、退出码、产物落盘 | `assert_cmd` 起子进程 |

同一行为**只在够得着它的最低那档测**。

## 怎么写

- crate 同时产出 `lib` 与 `bin`。业务逻辑一律测 `lib`；只有输出流、退出码、落盘位置才起二进制
- 测试必须离线。适配器换成假实现，`review()` 就能整套跑完
- 契约档：假 `RepoSource` / `WorktreeSource` / `protocol` / `tool` 与真实现共用同一组用例
- 新增行为先归到某一档，再写代码——写不出单独可跑的测试就说明分层错了

```rust
// ❌ 为行号对齐起五阶段 + 假模型
// ✅ 在 merge 的单元测试里直接喂行号集合与模型给出的 (file, line)

// ❌ 去重断言「说的是同一类问题」
// ✅ 只在 path 相同、区间相交、正文规范化后逐字相同时合并
```

## 不要测的

- 「语义相同」的去重（判不了；正文不同就不合并）
- 压制型 prompt 注入（空列表是合法输出，测不出来）
- 评审结论进退出码（退出码只描述 reviewbot 自己跑得怎么样）
