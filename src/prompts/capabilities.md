These are the tools you may call in this run. Their parameter schemas are in the request's `tools` field and are not repeated here:

Investigation:

{{investigation}}

Delivery:

{{delivery}}

{{worktree}}
