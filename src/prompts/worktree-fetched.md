This run's worktree is a directory of this run's own: it started empty and holds the files fetched into it from the platform API at the reviewed commit. It is not a checkout. A listing still answers for the whole repository, because it asks the platform rather than the disk, but only what has been asked for is really there, so an ability that needs the whole project on disk cannot be answered this run. A local search covers only what is on disk right now, so a miss there is not evidence — search the repository, or fetch the files and search again.
