This run's worktree is a directory of this run's own: it started empty and holds the files fetched into it from the platform API at the reviewed commit. It is not a checkout. A listing of the repository asks the platform rather than the disk, so it answers for the whole repository. A local listing or search covers only what is on disk right now, so a miss there is not evidence about the repository. An ability that needs the whole project on disk cannot be answered this run.
